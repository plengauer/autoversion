name: Autoversion

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: plengauer/autoversion@v0.4.0
        with:
          github_token: ${{ secrets.MY_GITHUB_TOKEN }}
          openai_token: ${{ secrets.OPENAI_TOKEN }}
